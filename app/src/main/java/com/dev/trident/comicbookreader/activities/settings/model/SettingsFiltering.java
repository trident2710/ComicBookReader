package com.dev.trident.comicbookreader.activities.settings.model;

/**
 * trident 15/04/2017.
 * Enum defining the different page filtering versions
 * for the
 */

public enum SettingsFiltering {
    /**
     * Page filtering for better text look
     */
    TEXT,
    /**
     * Page filtering for better image look
     */
    IMAGE
}
